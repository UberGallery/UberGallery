{# <header id="header" class="bg-black shadow sticky top-0 z-50">
    <div class="container flex justify-between flex-wrap items-center text-white mx-auto p-4">
        <h1>{{ title }}</h1>

        <div>Logo</div>

        <div>Menu</div>
    </div>
</header> #}

<header id="header" class="container border-b-2 border-black text-center mx-auto p-4">
    <h1 class="font-poiretone text-4xl">
        {{ title }}
    </h1>
</header>
